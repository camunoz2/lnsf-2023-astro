---
import Layout from "../layouts/Layout.astro";
import contacts from "../config/contacts.json";
import Footer from "../components/Footer.astro";
---

<Layout title="Contacto">
  <section class="max-w-2xl mx-auto py-8 px-4">
    <h1 class="text-3xl font-bold mb-4">Contacto</h1>
    <p class="mb-6">
      Si necesitas comunicarte con el equipo directivo del Liceo Nibaldo
      Sepúlveda, aquí encontrarás la información de contacto de las principales
      autoridades. Puedes escribirles directamente a sus correos electrónicos
      para consultas, sugerencias o solicitudes.
    </p>
    <ul class="space-y-6">
      {
        contacts.map((contact) => (
          <li class="border rounded-lg p-4 shadow">
            <h2 class="text-xl font-semibold">{contact.role}</h2>
            <p class="mb-1">{contact.name}</p>
            <a href={`mailto:${contact.email}`} class="text-blue-600 underline">
              {contact.email}
            </a>
          </li>
        ))
      }
    </ul>
  </section>
  <div class="container mx-auto px-4 py-12">
    <Footer />
  </div>
</Layout>
